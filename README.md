# SageBat

Adding on 384kHz ultrasound microphone to Sage node; working on acoustic detection, species identification, and isolating bat chirps from background noise (chirps = ms micro events, start with band-pass filtering & energy-based onset detection). Could also be a trigger to the mobotix (thermal) modality on the same node. 
